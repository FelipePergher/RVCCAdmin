@model PaginatedList<LigaCancer.Data.Models.PatientModels.TreatmentPlace>
@using LigaCancer.Code
@{
    ViewData["Title"] = "Cidades";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main class="main">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="/">Home</a>
        </li>
        <li class="breadcrumb-item">
            Cidades
        </li>
    </ol>
    <div class="container-fluid">
        <div id="ui-view" style="opacity: 1;">
            <div class="animated fadeIn">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="card card-default">
                            <div class="card-header">
                                Filtro
                            </div>

                            <form asp-action="Index" method="get">
                                <div class="card-block ">
                                    <div class="form-horizontal">
                                        <div class="form-group row">
                                            <label class="col-md-3 form-control-label">Cidade</label>
                                            <div class="col-md-9">
                                                <input class="form-control" name="searchCityString" value="@ViewData["CurrentSearchCityFilter"]" type="text">
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="card-footer ">
                                    <button type="reset" class="btn btn-sm btn-danger"><i class="fa fa-ban"></i>Resetar</button>
                                    <button type="submit" class="btn btn-sm btn-primary"><i class="fa fa-dot-circle"></i> Enviar</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12">
                        <div class="card">
                            <div class="card-header">
                                <i class="fa fa-address-card-o"></i> Cidades
                            </div>
                            <div class="card-block">
                                <div class="form-inline">
                                    <div class="form-group">
                                        <div class="btn-group">
                                            <a asp-action="AddTreatmentPlace" data-toggle="modal" data-target="#modal-action-treatmentPlace" class="btn btn-primary">
                                                <i class="glyphicon glyphicon-plus"></i> Adicionar Cidade
                                            </a>
                                        </div>
                                    </div>
                                </div>

                                <table class="table table-bordered table-striped table-condensed">
                                    <thead>
                                        <tr>
                                            <th>
                                                <a asp-action="Index" asp-route-sortOrder="@ViewData["CitySortParm"]" asp-route-page="@Model.PageIndex">
                                                    @if (ViewData["CitySortParm"].ToString() == "name_desc")
                                                    {
                                                        <span class="fa fa-sort-alpha-down"></span>
                                                    }
                                                    else
                                                    {
                                                        <span class="fa fa-sort-alpha-up"></span>
                                                    }
                                                    @Html.DisplayNameFor(model => model.FirstOrDefault().City)
                                                </a>
                                            </th>
                                            <th>
                                                Ações
                                            </th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.City)
                                                </td>
                                                <td>
                                                    <a asp-action="EditTreatmentPlace" asp-route-id="@item.TreatmentPlaceId" data-toggle="modal" data-target="#modal-action-treatmentPlace" class="btn btn-warning">
                                                        Editar
                                                    </a>
                                                    <a asp-action="DeleteTreatmentPlace" asp-route-id="@item.TreatmentPlaceId" data-toggle="modal" data-target="#modal-action-treatmentPlace" class="btn btn-danger">
                                                        Deletar
                                                    </a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>

                                @{await Html.RenderPartialAsync("_PaginationTreatmentPlace", Model);}

                            </div>
                        </div>
                    </div>
                    <!--/col-->

                </div>
                <!--/row-->

            </div>
        </div>
    </div>
    <!-- /.conainer-fluid -->
</main>

@await Html.PartialAsync("_Modal", new BootstrapModel { Id = "modal-action-treatmentPlace", AreaLabeledId = "modal-action-treatmentPlace-label", Size = ModalSize.Medium })

@section scripts
    {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script src="~/js/treatmentPlace.js"></script>
}